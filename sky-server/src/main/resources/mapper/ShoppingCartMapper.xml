<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.ShoppingCartMapper">

    <select id="list" parameterType="com.sky.entity.ShoppingCart" resultType="com.sky.entity.ShoppingCart">
        select * from shopping_cart
        <where>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="setmealId != null">
                and setmeal_id = #{setmealId}
            </if>
            <if test="dishId != null">
                and dish_id = #{dishId}
            </if>
            <if test="dishFlavor != null">
                and dish_flavor = #{dishFlavor}
            </if>
        </where>
    </select>

    <update id="updateNumberById" parameterType="com.sky.entity.ShoppingCart">
        update shopping_cart set number = #{number} where id = #{id}
    </update>

    <insert id="insert" parameterType="com.sky.entity.ShoppingCart">
        insert into shopping_cart
            (name, image, user_id, dish_id, setmeal_id, dish_flavor, amount, number, create_time)
        VALUES
            (#{name}, #{image}, #{userId}, #{dishId}, #{setmealId}, #{dishFlavor}, #{amount}, #{number}, now())
    </insert>

    <delete id="delete" parameterType="com.sky.entity.ShoppingCart">
        delete from shopping_cart
        <where>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="setmealId != null">
                and setmeal_id = #{setmealId}
            </if>
            <if test="dishId != null">
                and dish_id = #{dishId}
            </if>
            <if test="dishFlavor != null">
                and dish_flavor = #{dishFlavor}
            </if>
        </where>
    </delete>

    <delete id="cleanByUserId" parameterType="java.lang.Long">
        delete from shopping_cart where user_id = #{currintId}
    </delete>

</mapper>