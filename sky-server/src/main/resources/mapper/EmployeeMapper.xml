<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.EmployeeMapper">
    <resultMap id="dataMap" type="com.sky.entity.Employee">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="name" property="name"/>
        <result column="password" property="password"/>
        <result column="phone" property="phone"/>
        <result column="sex" property="sex"/>
        <result column="id_number" property="idNumber"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="create_user" property="createUser"/>
        <result column="update_user" property="updateUser"/>
    </resultMap>
    
    <select id="getByUsername" parameterType="java.lang.String" resultMap="dataMap">
        select * from employee where username = #{username}
    </select>

    <insert id="insert" parameterType="com.sky.entity.Employee">
        insert into employee
            (id, username, name, password, phone, sex, id_number, create_time, update_time, create_user, update_user)
        values
            (#{id}, #{username}, #{name}, #{password}, #{phone}, #{sex}, #{idNumber}, now(), now(), #{createUser}, #{updateUser})
    </insert>

    <select id="queryPage" parameterType="com.sky.entity.Employee" resultMap="dataMap">
        select * from employee
        <where>
            <if test="name != null and name != ''">
                and name like concat('%', #{name}, '%')
            </if>
        </where>
        order by create_time desc
    </select>

    <update id="update" parameterType="com.sky.entity.Employee">
        update employee
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="name != null">name = #{name},</if>
            <if test="password != null">password = #{password},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="sex != null">sex = #{sex},</if>
            <if test="idNumber != null">id_number = #{idNumber},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateUser != null">update_user = #{updateUser},</if>
            update_time = now(),
        </set>
        where id = #{id}
    </update>

    <select id="getById" parameterType="java.lang.Long" resultMap="dataMap">
        select * from employee where id = #{id}
    </select>

</mapper>
